version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "fmw_wls"
    path: "fmw_wls.yaml"

  - name: "os"
    path: "os/%{facts.os.family}/%{facts.os.release.major}.yaml"


    
